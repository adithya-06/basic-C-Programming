//Addition of two numbers
#include <stdio.h>
#include <conio.h>
void main()
{
    int a;
    int b;
    int sum;
    printf("Enter the Two numbers");
    scanf("%d, %d", &a, &b);
    sum = a + b;
    printf("The sum of the Two numbers is %d", sum);
    getch();
}
